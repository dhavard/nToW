package convert;

/**
 * Interface for conversion classes.
 */
public interface Converter {

    /**
     * Convert the input String into another String.
     *
     * @param input The String to convert
     * @return The converted string
     */
    String convert(String input);
}
